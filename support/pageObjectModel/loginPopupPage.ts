// Will contain the all the UI elements for the Login Page

// Log In label test ID, verify if element exist in the UI
// Email text field test ID, verify if element exist in the UI, function to set Email address
// Password text field test ID, verify if Password exist in the UI, function to set Password
// Log in button test ID, verify if Log In button exist and active in the UI, ability to click on it
// Sign up link test ID, verify if Sign Up link exist and active in the UI, ability to click on it
// Forgot your password, verify if Forgot your password link exist and active in the UI, ability to click on it

// additional elements and functionality will be
// Error message trigger, assuming it will be an inline error message
