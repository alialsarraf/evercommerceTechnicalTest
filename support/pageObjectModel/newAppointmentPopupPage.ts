// Will contain the all the UI elements for the new appointment pop up Page

// New Appointmen label test ID, verify if element exist in the UI
// Appointment name text field test ID, verify if element exist in the UI, function to set appointment name
// Date text field test ID, verify if element exist in the UI, function to set date (this can also, be an additiona popup up for date selector, for simplicity sake, just a text field)
// Time text field test ID, verify if element button exist and active in the UI, function to set time by entering text
// Save button test ID, verify if Save button exist and active in the UI, ability to click on it
// Cancel link, verify if Forgot your cancel link exist and active in the UI, ability to click on it

// additional elements and functionality will be
// Error message trigger, assuming it will be an inline error message for any element on the page remains empty
