// This is taking into account that we are refering to the Page object Model class and calls the verify element exist and functionality in the POM files

it("Inline error message trigger when user leave all fields empty"){
    // The user click on save and inline error message is trigger
    // assuming it will trigger an inline for each empty field
}

it("Inline error message trigger when user leave invalid data in the text field"){
    // The user click on save and inline error message is trigger
    // assuming it will trigger an inline for each  field
}