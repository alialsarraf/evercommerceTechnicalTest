// This is taking into account that we are refering to the Page object Model class and calls the verify element exist and functionality in the POM files

it("User navigate through the calander page for future dates"){
    // I am assuming, that we can shift dates and see any stored
    // appointments (this can be setup either with test users with fixed data)
    // create fresh data in before steps
}